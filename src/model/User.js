export default class User {
    constructor(uid, email, photoURL, username) {
      this.uid = uid;
      this.email = email;
      this.photoURL = photoURL;
      this.username = username;
    }
}