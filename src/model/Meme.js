export default class Meme {
    constructor(id, fecha, photoURL, user, likes) {
      this.id = id;
      this.fecha = fecha;
      this.photoURL = photoURL;
      this.user = user;
      this.likes = likes;
    }
}